<h1><%= link_to "Bands", bands_url %> » <%= @band.name %></h1>
<h2>Albums</h2>
<b><%= link_to "Add more album", new_band_album_url(@band) %></b>
<ul>
	<% @albums.each do |album| %>
		<li><%= link_to album.name, album_url(album) %>
			<%= button_to "Delete", band_album_url(@band, album), { method: :delete, action: "destroy" } %></li>
	<% end %>
</ul>
